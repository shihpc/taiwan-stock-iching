# B2｜個股三期間逐項參數字典

版本：P1-B2 v1　日期：2026-09-08
基準：v1.2.2 §4 ＋ S1 §A2 ＋ S1a §3 ＋ P0-A 報告
狀態：**規格文件。所有 c／d／權重皆為候選假說，未經回測驗證。**

**優先序（R5d 新增，R5g 加入 B5；五份 P1 文件逐字相同）**：`P1-B5` ＞ `P1-B4` ＞ `P1-B3` ＞ `P1-B2` ＞ `P1-B1` ＞ `S1a` ＞ `S1` ＞ `v1.2.2`（右者為左者的基準，衝突時**以左者為準**）。**凡「per X」的鍵／維度宣告，一律以 `P1-B5-dimensions.md` 為唯一事實來源。**
v1.2.2 §4（參數表）、§5（權重）、**§9.2（環境×門檻×名額，已被 S1 §A1 全面取代——含「門檻加 10 分」、五陽條件、名單上限 20／5／10／20；R5g 補）**內凡與 P1 重疊者，**一律以 P1 為準**；該三節保留原文僅作沿革，**不得據以實作**。
已知會做出不同參數的衝突點（照 v1.2.2 實作即錯）：個股二爻距離（v1.2.2 只有 MA20／MA60、d 1／1.5 ← B2.2 為 MA5/10/20/60、0.6/0.8/1.0/1.5）、個股二爻斜率（v1.2.2 寫死 MA20 五日變化、d=0.5 ← B2.2 為 MA_長 n 日變化、d 待 P2 校準）、五爻外資（v1.2.2 為 20 日 d=2%／5 日 d=4% ← B2.5 為三期間各一組）、五爻持續性（v1.2.2 只有 10 日 d=2 ← B2.5 中期為 20 日 d=3.34）、四爻序 1 回撤條件（v1.2.2 `5日回撤 ≤ 2` ← S1 §A2.2 `0 < n日回撤 ≤ 2`）、**個股三爻**（v1.2.2 單一組 20日 d=5pp／5日 d=3pp、產業 d=5pp、加速度「近5−前5」d=3pp ← B2.3 三期間各一組：產業 4/5/8pp、加速度 3/4/5pp）、**個股四爻族 C 延續**（v1.2.2 寫死「突破後第 3 日」 ← B2.4 為 3/5/10 日、基準 20/60/120 日最高收盤）、**個股四爻族 A 中期**（v1.2.2 §4.3 三期間都用情境表 ← B2.4 中期改用 `ln(漲跌日均量比)`＋OBV 斜率；註：v1.2.2 §5.1 與 B2.4 一致，是 §4.3 與 §5.1 自己不一致）、**個股初爻短線**（v1.2.2 三月合計 YoY d=15pp ← B2.1 短線改最新單月 YoY d=20pp）。（R5e 補：前五點為 R5d 所列，後四點為 R5e 驗收另行找到；此清單**未必窮盡**，實作時以優先序為準、不得回頭引用 v1.2.2 §4／§5 的參數。）


---

## B2.0 共用定義與全域規則

| 符號 | 定義 |
|---|---|
| `C_t, H_t, L_t, V_t` | 個股收盤／最高／最低／成交量（張） |
| `ATR14_{t−1}` | ATR14，**取 T−1 為止**（避免自我參照） |
| `VMA20_{t−1}` | 20 交易日均量，**不含當日** |
| `量比_t` | `V_t ÷ VMA20_{t−1}` |
| `日變動_t` | `(C_t − C_{t−1}) ÷ ATR14_{t−1}`（無因次） |
| `n日回撤_t` | `(max(C_{t−n+1..t}) − C_t) ÷ ATR14_{t−1}`（無因次、非負） |
| `S(x; c, d)` | 100／(1＋exp(−k(x−c)))，k＝ln(7/3)/d；`clip_3d`，值域 [7.30, 92.70] |
| `L(x; x₂₀, x₅₀, x₈₀)` | 三點線性映射，夾在 [10, 90] |
| `P_hist / P_cs` | 自身 250 日百分位／當日全池百分位 |

**六條全域規則**

1. **`clip_policy` 預設 `clip_3d`**；情境門檻與旗標判定用**未截斷原值**（截斷政策與「未截斷原值」出處＝**v1.2.2 §4.1a**；缺值分流（`denominator_zero`／`missing`／`insufficient_history`，見下方第 3 點）二分的出處＝**S1 §A2.3**，第三碼由裁定 #27 擴充。2026-09-08 交叉檢查更正：原引用 S1a §3.1 有誤，該節講的是旗標多空效果）。
2. **價格與 ATR 採一致還原口徑**（同為還原價或同為原始價，不可混用）。
3. **缺值原因碼三分（2026-09-12 裁定 #27，由 S1 §A2.3 的二分擴充；依優先序 P1-B2 ＞ S1，以本項為準）**：
   `denominator_zero`（分母為零）／`missing`（抓取失敗或來源未提供）／
   **`insufficient_history`（序列存在、但有效樣本長度不足以計算該指標）**，三者**分開統計**。
   第三碼的語意差別是**時間會自己解決**——把它混進 `missing` 會讓上線初期與暖機期的覆蓋率報表
   看起來像資料源故障。典型情形：`P_hist(250)`／百分位門檻要 250 個交易日而序列只有 130 個
   （2026-09 的 VIX 即是，見裁定 #26）。本碼**不是新造的**：實作端 `src/iching/score/transform.py`
   的 `REASON_INSUFFICIENT = "insufficient_history"` 早已在用，本項是補上規格背書、統一字面值。
   **本項只規範這三碼，不是原因碼全集**：實作端另有 `stale`／`contract_rolled`／`not_eligible`／`line_unknown`
   在用而尚未入規格（2026-09-12 驗收查出，見 `docs/P2-KICKOFF.md` 裁定 #27 的範圍外記錄）。**覆蓋率報表
   不得只開三個桶**，須能容納實作實際產出的原因碼、未知碼另計並告警。
4. **禁止本機日**，一律顯式台北時區（P0-A §1.1，Hetzner 為 UTC）。
5. **超額報酬的基準期間與該子指標視窗一致**：10 日超額用 10 日基準報酬，不得混用（本文件補明，原 §5 未定）。
6. **基準選擇**：上市對加權、上櫃對櫃買；產業基準取同交易所產業別的**中位數**（非平均，避免極端股拉動）。

**族權重缺值重配**：與 B1.7 同——族內缺取其餘平均；整族缺則其餘族按原比例正規化並記 `coverage_ratio`；某爻 `coverage_ratio < 0.5` → 該爻「未知」，卦名標「待補」。

---

### d 的校準政策（P1 定政策、P2 定值）

> 本節在 `P1-B1-market.md` §B1.0 與 `P1-B2-params.md` §B2.0 **逐字相同**，改動需兩處同步。

1. **P1 不宣稱任何縮放法則。** 先前寫過的 `d(n) = 0.5 × √(n/5)` 與更早的「d 與持有期間線性綁定」**皆已作廢**——實測顯示 MA 增量高度自相關（TAIEX 288 交易日：`|MA20 n 日變化| ÷ ATR14` 中位數 n=5→0.62、10→1.11、20→2.02，比值約 1.8，**接近線性 ∝ n，不是 √n**），√n 低估、n 為趨勢市上界，**猜哪個都沒有依據**（CANON 第 8 條：新指標先問有沒有回測依據）。
2. **凡視窗長度隨期間變動的子指標，其 `d` 必須是視窗長度的函數，不得三期間共用單一值。** P1 一律寫成 `d(n) ＝ 待 P2 校準`，並附一個**僅供管線跑得起來的起點值**；所有起點值一律 `calibrated=false`，**不得當成已驗證值引用**。
3. **P2 校準的唯一判準**：對每個子指標，取**訓練段樣本**上 `|x − c|` 的 **p85** 分位數（**母體定死為訓練段**：暖機資料只用於算出訓練段第一天的指標值、**不進 p85 母體**，驗證段／保留段亦不進；R5b 統一——本點原寫「暖機樣本」、第 4 點寫「訓練段」，兩說並存會做出不同的 d），令 `3d ＝ p85`，即 **`d ＝ p85 ÷ 3`**。選 p85 的理由：`clip_3d` 的截斷邊界正是 `c ± 3d`，令 `3d ＝ p85` 使**約 15% 的樣本落在截斷區**——尾端仍被截以保持穩健，中段保留足夠鑑別力。
4. **驗收閘門（分訓練段與樣本外，兩者處置不同——2026-09-08 使用者裁定）**：
   - **訓練段**：`d = p85 ÷ 3` 的校準**只用訓練段資料**得出，作為**校準起點**。訓練段截斷比例 > 15% → 判定 `d` 過小、**必須重定**，不得帶著上線。
   - **樣本外（驗證段／保留段）**：截斷比例超標**只觸發檢查（人工複核並記錄），不自動重校準、不回頭改 `d`**。理由：依樣本外表現調參等於**以保留段調參**，會污染 §13 的 purge／holdout 政策，使回測結論失效。複核結論若是「該指標確實不適用」，應循正常流程降權或移除並**重跑整套回測**，而不是就地把 `d` 調大讓數字好看。
   此閘門對**所有**使用 `clip_3d` 的子指標一體適用，**含本文件未逐一點名者**。條文見 v1.2.2 §16.5「截斷比例」列。
5. **本政策的目的是不必逐案猜**：任何未被點名的同類缺陷（視窗變動而 d 共用、d 明顯偏離實際尺度）都會被第 4 點的閘門攔下。
5a. **（R5h 新增）值域有界且要求截斷不生效的子指標為第二個例外**：持續性類（B2.5 族 C）的 `d` 是**反推**出來的（中期 d=3.34 使 `3d = 10.02 ≥ 10`，以符合 v1.2.2 §4.1a「截斷永不觸發」的宣稱）。若照第 3 點一律 `d = p85 ÷ 3`，其設計意圖正是**讓約 15% 落在截斷區**，必然把 `d` 改小、截斷開始觸發，於是 §4.1a 的宣稱與 §16.5「可達邊界」步驟 1 依賴的「持續性截斷不生效」假設**同時失效**（可達區間會變寬）。故此類**不套 p85 判準**，`d` 固定為 `原始值域上界 ÷ 3`；P2 若判定該類應改走 p85，必須**同時**改 §4.1a 的宣稱與 §16.5 的可達邊界登錄，不得只改一邊。
6. **距離型子指標（`(X − MA_n) ÷ ATR14`）為例外，維持依 MA 窗長查表**（MA5 0.6／MA10 0.8／MA20 1.0／MA60 1.5）、與持有期間無關。理由：它是**水準型距離量、不是 n 日累積量**，其尺度隨 **MA 窗長**而非持有期間改變。此查表值同樣是未校準起點值，**待 P2 以同一 p85 判準覆核**。

---

### 轉換值域統一（normalize to S 的值域）

> 本節在 `P1-B1-market.md` §B1.0 與 `P1-B2-params.md` §B2.0 **逐字相同**，改動需兩處同步。

**問題**：各轉換的原生值域不等（`S`＋`clip_3d` 為 [7.30, 92.70]、`L` 為 [10, 90]、百分位與情境表為 [0, 100]），卻用線性權重相加、又套用全爻共用的門檻（陰陽 50、`F-臨界` [45, 55]、內外卦 ≥55／≤45）。**手算**各爻可達分數區間（**非實跑實測**；權威值以 v1.2.2 §16.5「可達邊界」列的演算法產出、寫入 `docs/score-ranges.md`，本處僅為說明問題的示例）：大盤三爻 [13.42, 80.58]、大盤五爻 [3.65, 96.35]、個股四爻 **[19.33, 80.68]**（R5e 更正：原寫 [17.32, 82.67] 是用 `L(0,.5,1)` 的**宣告**值域 [10,90] 算的，與本節下方「宣告值域 ≠ 可達值域」警告自相矛盾；以可達值域 [20,80] 重算為 [19.33, 80.68]）——大盤三爻**永遠到不了 81 分**，`F-分歧` 先天偏向外卦較易 ≥55。

1. **所有子指標的轉換輸出一律映射到 [7.30, 92.70]**，與 `S` 的 `clip_3d` 有效值域對齊。
2. **正規化函式**：`N(v; lo, hi) ＝ 7.30 + (v − lo) ÷ (hi − lo) × 85.40`，其中 `[lo, hi]` 是該轉換的**原生值域**。
3. **不變式（必須明寫）**：`N` 保持中性點在 50——原生值域的中點恆映到 **50.0**（驗算：`7.30 + 0.5 × 85.40 ＝ 50.0`）。故陰陽門檻 50、`F-臨界` [45, 55]、內外卦 ≥55／≤45 的**數值**維持不變、不改寫。
   **但「50 不動」不代表「45／55 的判斷等價」**（2026-09-08 使用者裁定，撤回本點原先「全部維持不變、不需重訂」的推論——那是錯的）：不動點只保證 50 映到 50，**不保證分數跨越 55／45 的頻率與時點不變**；而陰陽態、遲滯（連兩日 ≥55／≤45）、`F-臨界`、內外卦判定吃的正是**跨越行為**，不是數值本身。**須重驗的項目共八項**（正本在 v1.2.2 §16.5「門檻行為重驗」列，此處同步列出以免只讀本檔者漏做）：①陰陽態逐日差異率 ②遲滯翻轉次數 ③各旗標觸發率 ④動爻數分布 ⑤內外卦 ≥55／≤45 的方向判定差異率（三爻聚合層，不等於逐爻陰陽態）⑥主卦與之卦 `king_wen` 的逐日一致率（④只比動爻「數」，動爻**位置**換了卦就換了）⑦候選名單與排名的重疊率 ⑧**封頂／下限的 binding 率**（`max(base, 84.16)` 與封頂 79.89 的觸發率——`base` 本就在 [7.30, 92.70]，把 90→84.16、85→79.89 **不是尺度換算而是實質改門檻**，③只涵蓋旗標、創高下限沒有任何一項在看）。**因此：所有因 `N`（或任何縮放）而改變的門檻行為，一律須重新驗證**——以同一段歷史樣本，比對縮放前後的**上列①–⑧全部八項**，差異須逐項記錄並解釋；未經此驗證者不得視為「語意不變」。（R5d 更正：本句原只重列①–④，與同點前段的「共八項」矛盾，實作者照這句做會少驗四項。）條文見 v1.2.2 §16.5「門檻行為重驗」列。
4. **逐轉換的原生值域**：

| 轉換 | 原生值域 | 套用方式 |
|---|---|---|
| `S` ＋ `clip_3d` | [7.30, 92.70] | **恆等映射，不重複套用** |
| `L(a, b, c)` 線性映射 | **宣告** [10, 90] | `N(v; 10, 90)` |
| `P_hist` 百分位 | [0, 100] | `N(v; 0, 100)` |
| `P_cs` 橫斷面百分位 | [0, 100] | **不套 `N`**（見下） |
| 情境表離散值（B1.3 族 C、B2.2 族 C、B2.4 族 A／族 C、B2.5 族 D） | [0, 100] | `N(v; 0, 100)` |
| 大盤方向分數（B2.6 族 A 直接沿用者） | 由已正規化的六爻分數加權而得，[7.30, 92.70] | **恆等映射，不重複套用** |

> ⚠ **宣告值域 ≠ 可達值域**（R5d 補，R5e 移到表下——原插在表格中間會切斷後半四列）：`N` 的 `[lo, hi]` 一律取**宣告值域**（映射規則固定、不隨資料變），但**可達邊界的計算必須用可達值域**（v1.2.2 §16.5「可達邊界」列步驟 1）。兩者常不同：`L(0, 0.5, 1)` 的輸入天然落在 [0, 1]、錨點就在定義域端點，[10, 90] 的夾制**永不生效**，實際只到 [20, 80]（過 `N` 後 [17.98, 82.03]）；同理持續性子指標因 `3d ≥ 原始值域上界`、截斷不生效，`S` 的可達界**窄於** [7.30, 92.70]。**不得拿宣告值域去算可達邊界**——這是 R5d 抓到的系統性錯誤。

5. **情境表離散值換算對照**（`N(v; 0, 100) ＝ 7.30 + v × 0.854`，供實作直接查）：

| 原生值 | 20 | 25 | 35 | 40 | 50 | 55 | 60 | 80 |
|---|---|---|---|---|---|---|---|---|
| 套 `N` 後 | 24.38 | 28.65 | 37.19 | 41.46 | **50.00** | 54.27 | 58.54 | 75.62 |

情境表中以公式給分的分支，**整段在原生尺度上算完、再套一次 `N`**（其內嵌的 `S(…)` 是原生尺度上的中間值、不是最終輸出，故不單獨套 `N`）：

| 出處 | 原生值域 | 套 `N` 後 |
|---|---|---|
| B2.4 族 A 序 2 上漲放量 | [60, 81.35] | [58.54, 76.77] |
| B2.4 族 A 序 3 下跌放量 | [18.65, 40] | [23.23, 41.46] |
| B2.5 族 D 序 3（資增價跌） | [7.30, 47.88] | **[7.30, 47.88]（恆等）** |
| B2.5 族 D 序 4（資減） | [51.06, 71.35] | **[51.06, 71.35]（恆等）** |

`L` 的三個錨點換算（`N(v; 10, 90) ＝ 7.30 + (v − 10) × 1.0675`）：20→**17.98**、50→**50.00**、80→**82.03**。

6. **已做過反向者**（如 B1.5 族 C 的 `100 − P_hist`）：**反向在原生尺度上做完、再套 `N`，順序不得顛倒**。
6a. **`P_cs` 是唯一不套 `N` 的轉換**（2026-09-08 補）：v1.2.2 §4.3 明定 `P_cs`「**只進排名層與旗標，不進爻分**」。既不進爻分，就沒有與其他子指標同尺度相加的需求；而它的消費端全是**原生 0–100 的門檻**（過熱旗標的 `P_cs ≥ 95`、S1 §B2「依 `P_cs` 排名取前 N 名」），套了 `N` 會讓 95 變成 88.43 而門檻仍寫 95，**排名與旗標一起錯**。故 `P_cs` 一律保持原生 0–100、不宣告 `native_range` 的映射。
7. **不得對同一個值重複套用 `N`。** 實作上每個子指標宣告 `native_range`（併入 §B2.8 參數字典欄位），由框架統一套用一次；`native_range ＝ [7.30, 92.70]` 者即恆等映射。
8. **0–100 直覺常數一律過 `N`（2026-09-08 補齊）**：凡照 0–100 直覺挑、卻要與**已正規化**的分數比較的常數，一律視為原生值並過 `N(v; 0, 100)`。已套用者：B2.1 創高下限 `90 → **84.16**`、B2.3 過熱封頂 `85 → **79.89**`。**例外（水準門檻，一律不換算）**：陰陽門檻 50、`F-臨界` [45, 55]、內外卦 ≥55／≤45、**以及所有拿 0–100 常數去比「已正規化爻分數」的水準條件**——已知者：B2.4 族 A 序 1 的「二爻分 ≥ 55」（同 S1 §A4）。判準：**該常數若是圍繞中性點 50 的水準比較 → 不換算；若是子指標自身的給分、封頂或下限 → 換算**。理由：這些是圍繞中性點的**水準**門檻，換算後仍是自己（`N(50)=50.00`），改寫數值只會製造 45→45.5 這種假精度。**但不換算 ≠ 行為不變**——其跨越行為仍須依第 3 點重新驗證。**變化量門檻另有處理，見第 9 點。**
9. **變化量門檻須按值域跨幅縮放（2026-09-08 補）**：`N` 保中性點但**改變值域跨幅**，故「分數變化 ≥ X」這類門檻不能原封不動。大盤二爻正規化前**手算**可達區間 [8.92, 91.08]（跨幅 82.16），正規化後為 [7.30, 92.70]（跨幅 85.40），比值 **1.0394**。已套用：`F-廣度擴張／收縮` 的「二爻分數 5 交易日變化 ≥ +5／≤ −5」→ **≥ +5.2／≤ −5.2**（`5 × 85.40 ÷ 82.16 = 5.20`）。此值為**未校準候選值（`calibrated=false`）**——跨幅比是由**手算**可達區間推得（非實跑實測），P2 須以實測分布重定。**水準門檻（第 8 點例外那三組）不適用本點。**
10. **聚合層門檻（`T0` 與門檻調整量）不套第 8／9 點，改列為 P2 校準對象**（R5c 補，補上第 8／9 點的分類缺口）：S1 §A1.2 的候選門檻 `T0`（60／65／70／75）與 §A1.3 的門檻調整（+5／+10／+20，上限 +20）是拿 0–100 直覺常數去比**已正規化的 `base_score`**，既不是「圍繞中性點 50 的水準比較」（第 8 點例外），也不是「子指標自身的給分／封頂／下限」（第 8 點換算），**兩類都不是**。
    - **不得沿用第 9 點的跨幅比 1.0394**——那是**大盤二爻**推出來的。實算個股 `base_score`（§5.2 短線權重）的可達跨幅為正規化前 78.92 → 後 **77.18，比值 0.978，方向相反**。
    - **也不做 0.978 這種換算**：`T0` 與 +5／+10／+20 本來就從未校準，乘一個 2% 的係數只是**在未校準值上疊加假精度**。
    - **正確處置（R5d 改寫）**：`T0` 與門檻調整量一律標 `calibrated=false`。**P2 判準＝訓練段 `base_score` 條件分布的分位數**：對 **`state` × `direction` × `horizon` × `market` ＝ 4×2×3×2 ＝ 48 格**（R5g 更正：原寫「4 狀態 × 多空 ＝ 8 個條件分布」，**漏掉期間與市場**——`base_score` 逐期間各一個、`d` 逐市場各一組；S1 §A1.1–A1.4 全節未出現期間維度，是 S1 §A1 取代 v1.2.2 §9.2 時把該維度弄丟的**退化**。第一版若只出短線名單，須寫死 `horizon='short'`、**不得省欄**，見 `P1-B5-dimensions.md` §B5.0 規則 3）的 **48 個條件分布**，各取同一個分位數 `q`（例如 p80）作為該格的 `T0`，**48 個值由 48 個分布各自產生、只需定一個 `q`**（R5h 更正：原寫 8／8）；`q` 的選取規則（R5e 補明確，原寫法必然多解）：**取滿足條件的最大 `q`**——「條件」＝**48 格全部成立**，每格以**該基本狀態實際出現的訓練段交易日**為母體（非全訓練段），各格用自己的 `N0`（S1 §A1.2：多方 20／12／10／5、空方 5／10／10／20），要求該格 ≥ 80% 的交易日其通過數 ≥ 該格 `N0`。通過數對 `q` 單調遞減，故可行集為 `[0, q*]`、**取 `q = q*`**；若 `q* = 0`（連不設門檻都達不到 `N0`）則判定 `N0` 訂太大，回頭調 `N0` 而非讓 `T0` 退化為無門檻。 **位移的夾制（R5g 補）**：`q*` 已是「剛好還能填滿 `N0`」的上界，任何旗標位移（只會 +）**必然使該格填不到 `N0`**，這是刻意的（旗標本就是收緊）；但 `q* + 位移` 可能 > 100，故**一律夾制在 `min(q* + Σ位移, 99)`**，且該格候選數不足 `N0` 時**不補足、據實輸出較短名單並記 `short_list=true`**，不得回頭放寬 `q`。門檻調整量（+5／+10／+20）改寫為**分位數位移**，不再是分數尺度上的常數——對照：**+5 → +0.5 個十分位、+10 → +1 個十分位、+20 → +2 個十分位**；套用規則「門檻取加總、上限 +20」隨之改為「**位移取加總、上限 +2 個十分位**」（R5f 補：原「加總、上限 +20」在分位數位移語意下不成立——無法把 +5 分加到分位數門檻再以 +20 分封頂；不補這句，實作者照 B1.8／S1 §A1.3 的分數尺度常數寫，必然回到舊語意）。**下游三張旗標表（`P1-B1-market.md` §B1.8、`stock-iching-S1-supplement.md` §A1.3、`stock-iching-S1a-annotations.md` §3.1）與 S1 §A1.2 的 `T0` 表，其數值一律 `calibrated=false`。**
11. **事件層 `E` 的級距同屬本類（R5g 補，補上第 8／9／10 點都沒涵蓋的下游）**：`event_shadow_score = base_score + E`，而 `E ＝ 方向 × 級距(5／10／15) × 可信度 × 衰減`（v1.2.2 §7）。級距 5／10／15 正是「照 0–100 直覺挑、卻要與**已正規化**分數相加」的常數。處置：①級距一律 `calibrated=false`，但**維持分數尺度、不改分位數位移**（R5h 裁定，解 R5g 版的自我矛盾：`T0` 是**門檻**、換成分位數合理；`E` 是**加到分數本體**的量，換成分位數後 `base_score + E` 型別不成立、②的值域守門與聚合量登錄同時失去意義）。P2 校準判準＝以「事件後 h 日異常報酬」的效果量定級距，與 `T0` 的分位數判準各走各的；②`event_shadow_score` **必須有值域守門**——§16.5「合法範圍」列原只驗爻分數，R5g 已補 shadow 列：`base_score + E` 亦須落在 [7.30, 92.70]，逸出即夾制並記 `shadow_clipped=true`；③`event_shadow_score` 的可達區間納入 `P1-B5-dimensions.md` §B5.2 的「聚合量可達區間」登錄。
      **不採「以目標名單規模反推 `T0`」**（R5d 驗收指出三個致命問題，逐字保留以免再提）：①**架空**——S1 §A1.4 步驟 5 是「過 `T0` 的候選再依 `P_cs` 取前 `N0` 名」，只要通過數 > `N0`，實際名單規模恆等於 `N0`、與 `T0` 無關，反推只在 `T0` 嚴到通過數 < `N0` 時才有解，那等於讓 `T0` 取代 `N0` 的功能；②**目標不存在**——§9.2 的 20／5／10／20 是**名額上限**且已被 S1 §A1.2 的 `N0` 取代，全案沒有定義過「平均檔數的目標區間」；③**一個目標定不了 48 個值**（R5i：原寫「八個」，是 8 格改 48 格前的沿革殘句）。改用分位數判準後：`T0` 恢復「絕對品質下限」的角色、`N0` 維持「容量上限」，兩者不再互相架空。
      **選取 `q` 所用資料**限訓練段（R5e：「反推」是已廢棄判準的措辭殘留；與 §13 的 purge／holdout 政策一致）。行為變化由第 3 點重驗項⑦（候選名單與排名重疊率）涵蓋。

---

## B2.1 初爻｜營運基礎

| 族 | 權重 | 子指標 | 公式 | 單位 | c | d | 方向 |
|---|---:|---|---|---|---|---|---|
| A 營收 | .50 | 三月合計 YoY | 最近 3 月合計 ÷ 去年同期 3 月合計 − 1 | pp | 0 | 15 | + |
| | | 營收加速度 | 近 3 月合計 YoY − 前一組 3 月合計 YoY | pp | 0 | 10 | + |
| | | 近 12 月創高 | 最新月營收為近 12 月最高 → 族分取 **`max(base, 84.16)`**（`base` ＝族 A 其餘子指標平均；**下限而非上限**；84.16 ＝ `N(90; 0, 100)`，見 §B2.0 政策第 8 點） | 0/1 | — | — | + |
| B 獲利 | .30 | 季 EPS YoY | 僅在**前期 EPS > 0.1 元**時使用 | pp | 0 | 20 | + |
| | | EPS 差額÷股價 | **替代指標**，用於前期 EPS ≤ 0.1 元或由負轉正：`(本期EPS − 去年同期EPS) ÷ 期末股價` | % | 0 | 2 | + |
| | | 毛利率 QoQ | 本季毛利率 − 上季毛利率 | pp | 0 | 1 | + |
| C 產業相對 | .20 | 三月 YoY − 產業中位 | 同族 A 定義的三月 YoY 減去同產業中位數 | pp | 0 | 10 | + |

> **創高規則為「下限」不是「上限」**（2026-09-08 交叉檢查更正）：族 A 兩子指標同時滿分時 `base` 可達 **92.70**（S 截斷後上界），原寫成「上限 90」會使**創高反而扣 2.7 分**。改為 `score = max(base, 84.16)`：創高只保證不低於 84.16（＝原生 90 過 `N` 後的值），不壓低本來就更高的分數。

**三期間差異**

| 期間 | 族 A 視窗 | 族 B | 族 C |
|---|---|---|---|
| 短線 | **最新單月 YoY**（d=20pp）＋加速度 | 不納入（權重移給 A） | 不納入 |
| 波段 | 三月合計 YoY ＋加速度 | 不納入 | 不納入 |
| 中期 | 三月合計 YoY ＋加速度＋創高 | 全部 | 全部 |

短線與波段的初爻只有族 A，權重 1.0；中期為 .5／.3／.2。理由：季獲利與產業比較的更新頻率（季）遠低於短線視窗，納入只會增加延遲不增加資訊。

**金融保險業替代規則**（無毛利率概念）：族 B 改為「稅前淨利 YoY」（c=0, d=20pp）與「淨值 QoQ」（c=0, d=2%），族 A 若無月營收則整族缺、權重重配。以 `TaiwanStockInfo.industry_category` 判定。

**時間對齊（P0-A §4.2、§4.3 修正）**

| 資料 | `available_at` 規則 |
|---|---|
| 月營收 | `create_time` **嚴格大於 2026-04-21** 者：該日結束後、下一可交易時點可用。`create_time` 等於 2026-04-21 或更早（回填值）：**回落到「所屬月次月 10 日收盤後」的保守近似**。**回測須做 +3、+5 交易日的延遲敏感度**。 |
| 季財報 | **FinMind 無公布日欄位（已驗證）**，`date` 是期別末日。一律採法定期限近似：Q1→5/15、Q2→8/14、Q3→11/14、年報→3/31，**收盤後可用**；同樣做延遲敏感度。遲報與修訂無法辨識，**列為回測限制**。 |

**來源**：`TaiwanStockMonthRevenue`、`TaiwanStockFinancialStatements`、`TaiwanStockInfo`。

---

## B2.2 二爻｜價格趨勢

| 族 | 權重 | 子指標 | 公式 | c | d | 方向 |
|---|---:|---|---|---|---|---|
| A 距離 | .40 | (C − MA_短) ÷ ATR14 | 見下表 | 0 | 依 MA 窗長查表（下） | + |
| | | (C − MA_長) ÷ ATR14 | 見下表 | 0 | 依 MA 窗長查表（下） | + |
| B 斜率 | .30 | MA_長 n 日變化 ÷ ATR14 | 見下表 | 0 | `d(n)` 待 P2 校準，起點值 短線 0.5／波段 0.7／中期 1.0（`calibrated=false`，見註） | + |
| C 結構 | .30 | 已確認高低點結構 | HH＋HL→80、LH＋LL→20、其他→50（**原生值**，計分時過 `N(v; 0, 100)` → 75.62／24.38／50.00） | — | — | + |

| 期間 | MA_短 | MA_長 | 斜率視窗 | 結構視窗 |
|---|---|---|---|---|
| 短線 | MA5 | MA20 | 5 日 | 20 日 |
| 波段 | MA10 | MA20 | 10 日 | 20 日 |
| 中期 | MA20 | MA60 | 20 日 | 60 日 |

**族 A 距離的 d ＝依 MA 窗長查表，與期間無關**（與 B1 §B1.1 大盤同一張表、同值）：

| MA 窗長 | MA5 | MA10 | MA20 | MA60 |
|---|---|---|---|---|
| d | 0.6 | 0.8 | 1.0 | 1.5 |

> **d 的綁定方向已定案（2026-09-08 交叉檢查，使用者裁定「甲」）**：`d` 是**指標尺度的性質，不是持有期間的性質**——`(C − MA20)/ATR14` 這個量的分布不會因為誰在看它而改變。故 d 一律綁 **MA 窗長**，B1（大盤）與 B2（個股）**共用同一張查表、同值**，實作只需一份設定。
> 兩次更正的歷程（留存以免再翻回去）：①原表綁「MA_短 1.0／MA_長 1.5」，使同一條 `(C − MA20)/ATR14` 在短線 d=1.5、中期 d=1.0（反向）。②中間版本改綁「期間 1.0／1.2／1.5」，但同期間內 MA_短與 MA_長仍共用一個 d，與其自稱的理由（長窗波動大→d 大）自相矛盾，且與 B1 的窗長綁定相反。**兩版皆已作廢。**
> MA5 與 MA10 的 d 為**未校準候選值（`calibrated=false`），待 P2 校準**；MA20／MA60 沿用 v1.2.2 既有值。**四個值全部待 P2 以 §B2.0「d 的校準政策」第 3 點的 p85 判準覆核。**

> **族 B 斜率的 d 依窗長而定、值待 P2 校準（2026-09-08 R5 修訂）**：`MA_長 n 日變化 ÷ ATR14` 是 **n 日累積量**，尺度隨窗長 5／10／20 日放大，**三期間不得共用單一 d**。表中 0.5／0.7／1.0 **僅為起點值**（`calibrated=false`，僅供管線跑得起來）；**產生它們的 `d(n) = 0.5 × √(n/5)` 法則已作廢**（§B2.0「d 的校準政策」第 1 點），P2 依 `d = p85 ÷ 3` 逐期間重定，受 >15% 截斷閘門管轄。
> **⚠ 本族與 B1 §B1.1 大盤斜率族不得共用設定物件、必須各自校準、各自一份設定**：個股斜率算的是 **MA_長**（中期＝**MA60**），大盤斜率恆為 **MA20**，**不是同一個量**——實測 TAIEX 20 日變化除以 ATR14 的中位數，MA20 ≈ 2.02、MA60 ≈ 0.86（差約 2.35 倍），共用 d 會使兩者截斷比例差一倍（32.2% vs 16.3%）。此處**不適用**族 A 距離的「B1／B2 同一張表」慣例（該慣例只涵蓋**距離型**子指標）。

**結構的擺動點定義**（原未明定，本文件補）：以 `k = 2`（短線）／`3`（波段）／`5`（中期）根 K 為窗，`H_t` 為前後各 k 根的最高 → 記為波峰；`L_t` 為前後各 k 根的最低 → 波谷。**必須有 k 根後續 K 才能確認**，故最新的波峰谷有 k 日延遲，**不得用未確認的擺動點**。取最近兩個波峰與兩個波谷判 HH／HL。

---

## B2.3 三爻｜相對動能

| 族 | 權重 | 子指標 | c | d | 方向 |
|---|---:|---|---|---|---|
| A 相對市場 | .50 | 長視窗超額報酬 | 0 | 見下表 | + |
| | | 短視窗超額報酬 | 0 | 見下表 | + |
| B 相對產業 | .25 | 長視窗超額（對產業中位） | 0 | 見下表 | + |
| C 加速度 | .25 | 兩段等長窗超額之差（各期間窗長見下表） | 0 | 見下表 | + |

| 期間 | 短視窗 / d | 長視窗 / d | 產業 d | 加速度定義 / d |
|---|---|---|---|---|
| 短線 | 5 日 / 3pp | 10 日 / 4pp | 4pp | 近 5 日 − 前 5 日 / 3pp |
| 波段 | 10 日 / 4pp | 20 日 / 5pp | 5pp | 近 10 日 − 前 10 日 / 4pp |
| 中期 | 20 日 / 5pp | 60 日 / 8pp | 8pp | 近 20 日 − 前 20 日 / 5pp |

> **族 C 加速度的兩段窗長（2026-09-08 交叉檢查更正，取代原「近半段 − 前半段」的歧義措辭）**：短線 ＝ 最近 5 日超額 − 其前 5 日超額（共 10 日）｜波段 ＝ 最近 10 日超額 − 其前 10 日超額（共 20 日）｜中期 ＝ **最近 20 日超額 − 其前 20 日超額**（共 40 日）。**中期的長視窗雖為 60 日，加速度仍只取最近的 20／20 兩段，最早的 20 日不計入**——「半段」在中期並非長視窗的一半，故改為明列窗長。

**過熱旗標**（三多凶）：`P_cs(長視窗超額) ≥ 95` **且** `(C − MA20) ÷ ATR14 > 3`（未截斷原值）→ 三爻分**封頂 79.89**（＝`N(85; 0, 100)`，見 §B2.0 政策第 8 點；2026-09-08 補：政策第 8 點原已宣稱此項「已套用」但本文未改，屬漏改），並標 `overheated=true`。

**產業樣本不足**：同產業有效樣本 < 5 檔 → 族 B 缺，權重重配。

---

## B2.4 四爻｜量價確認

### 族與權重

| 族 | 權重 | 內容 |
|---|---:|---|
| A 成交確認 | .50 | 情境表（S1 §A2.2 修訂版） |
| B 收盤位置 | .20 | **n 日均** `(C−L)/(H−L)`，`L(0, 0.5, 1)`；**n 依期間：5／10／20**（R5g 補：原寫死「5 日均」、三期間視窗從未定義，且 v1.2.2 §5.1 的波段與中期欄位裡根本沒有這個子指標——依優先序以本表為準）|
| C 延續 | .30 | 突破／跌破後守住與否 |

### 族 A 情境表（有序 if–elif，未截斷原值）

> **表中「分數」為原生值**（0–100 直覺尺度），計分時整段在原生尺度算完、再過一次 `N(v; 0, 100)`（見 §B2.0「轉換值域統一」）：離散值 60→58.54、50→50.00；公式分支 序 2 [60, 81.35]→**[58.54, 76.77]**、序 3 [18.65, 40]→**[23.23, 41.46]**。序 2／序 3 內嵌的 `S(量比−1; 0.3, 0.7)` 是**原生尺度上的中間值、不是最終輸出**，不單獨套 `N`。

| 序 | 情境 | 條件 | 分數 |
|---|---|---|---|
| 0 | 資料不足 | `ATR14` 或 `VMA20` 缺／為零；當日無成交 | **缺值** |
| 1 | 縮量回檔 | 二爻分 ≥ 55（**水準門檻，不換算**，見 §B2.0 政策第 8 點例外）**且 `0 < n日回撤 ≤ 2`** 且 量比 < 0.8 且 `C ≥ MA20` | **60** |
| 2 | 上漲放量 | 日變動 ≥ 0.5 且 量比 ≥ 1.3 | `60 + 0.5 × (S(量比−1; 0.3, 0.7) − 50)` → **60–81.35** |
| 3 | 下跌放量 | 日變動 ≤ −0.5 且 量比 ≥ 1.3 | `40 − 0.5 × (S(量比−1; 0.3, 0.7) − 50)` → **18.65–40** |
| 4 | 一般縮量 | 量比 < 0.8（未落入序 1） | **50** |
| 5 | 其他 | 不符以上 | **50** |

- 序 1 的 `n` ＝ 5（短線）／10（波段）／20（中期）。
- **一價成交（H＝L＝C，漲跌停鎖死）**：族 A 照算；只有族 B 該日不計入 5 日平均（分母為零）。**不得整爻判缺值。**
- 旗標：量比 ≥ 3 且 日變動 ≤ −1.5 → 風險層「異常長黑」；序 2 且 `(C−MA20)÷ATR14 > 3` → 風險層「追價」。族 A 分數不因旗標再變。

### 族 A 的三期間合成（原未定，本文件補）

| 期間 | 合成方式 |
|---|---|
| **短線** | `0.6 × 當日情境分 + 0.4 × 近 5 日情境分平均`（重當日但用 5 日平滑單日噪音） |
| **波段** | 近 10 日情境分平均 |
| **中期** | **改用兩個連續指標取代情境表**：①`ln(近20日上漲日均量 ÷ 下跌日均量)` → S(c=0, d=0.3)；②**OBV 20 日斜率**（見下），兩者各 .5 |

理由：情境表是「當日事件」判定，視窗越長越不適用；中期改用連續量能結構指標。

### 中期族 A 的 OBV 20 日斜率（原未定，本文件補）

```
OBV_t = OBV_{t−1} + sign(C_t − C_{t−1}) × V_t      （C_t = C_{t−1} 時計 0）
slope  = 對最近 20 個 OBV 值做最小平方線性迴歸的斜率（單位：張／日）
x      = slope ÷ VMA20_{t−1}                        （無因次，日均量的幾分之幾）
分數    = S(x; c=0, d=0.5)
```
`clip_3d`；`VMA20 = 0` → `denominator_zero`。

### 族 C 延續（原未定三期間，本文件補）

| 期間 | 突破基準 | 確認窗 | 判定 |
|---|---|---|---|
| 短線 | 近 20 日最高收盤 | 第 **3** 日 | 突破後第 3 日收盤仍 ≥ 突破價 → 80；跌破後第 3 日未收復 → 20；期間內無突破跌破 → 50 |
| 波段 | 近 60 日最高收盤 | 第 **5** 日 | 同上 |
| 中期 | 近 120 日最高收盤 | 第 **10** 日 | 同上 |

**確認窗未完成前一律 50**，不得提前使用（v1.2.2 §13.4：突破守穩到第 N 日完成才知道）。表中 80／20／50 為**原生值**，計分時過 `N(v; 0, 100)` → **75.62／24.38／50.00**（見 §B2.0「轉換值域統一」）。

---

## B2.5 五爻｜籌碼供需

| 族 | 權重 | 子指標 | 公式 | c | d | 方向 |
|---|---:|---|---|---|---|---|
| A 外資 | .30 | 長視窗淨買超強度 | 期間淨買超股數 ÷ 同期間成交股數 | 0 | 見下表 | + |
| | | 短視窗淨買超強度 | 同上 | 0 | 見下表 | + |
| B 投信 | .30 | 同族 A（分開計算） | 同上 | 0 | 見下表 | + |
| C 持續性 | .15 | 買超天數 − 中點 | 見下表 | 0 | 見下表 | + |
| D 融資 | .10 | **有序情境（見下）** | — | — | — | 條件式 |
| E 借券 | .15 | 借券賣出餘額期間變化 ÷ 流通股數（視窗依期間） | pp | 0 | `d(n)` 待 P2 校準，起點值三期間皆 **0.3**（`calibrated=false`） | **−** |

| 期間 | 短視窗 / d | 長視窗 / d | 持續性定義 / d | 融資、借券視窗 |
|---|---|---|---|---|
| 短線 | 3 日 / 5% | 5 日 / 4% | **近 5 日買超天數 − 2.5** / 1 天 | 5 日 |
| 波段 | 5 日 / 4% | 10 日 / 3% | **近 10 日買超天數 − 5** / 2 天 | 10 日 |
| 中期 | 10 日 / 3% | 20 日 / 2% | **近 20 日買超天數 − 10** / **3.34 天** | 20 日 |

族 A／B 內部：長視窗 .6、短視窗 .4。持續性以**外資**為準（投信買超天數波動大、意義弱）。

> **族 E 借券的 d 不得三期間共用（2026-09-08 R5 修訂）**：借券賣出餘額的**期間變化**是 n 日累積量，視窗 5／10／20 日，原寫的單一 d=0.3pp 會使中期大量落在截斷區。依 §B2.0「d 的校準政策」寫成 `d(n) ＝ 待 P2 校準`，三期間起點值暫皆為 **0.3pp**（`calibrated=false`），P2 依 `d = p85 ÷ 3` 分期間重定，受 >15% 截斷閘門管轄。族 A／B／C 的 d 本來就已分期間列出，維持不變、同受該閘門管轄。

> **中期持續性的 d 由 3.00 改為 3.34（2026-09-08 交叉檢查更正）**：中期持續性原始值域為 ±10（近 20 日買超天數 0–20 減中點 10），`clip_3d` 的截斷區間為 ±3d。d=3.00 時 ±3d ＝ ±9 **會觸發截斷**，與 v1.2.2 §4.1a「持續性截斷永不觸發」的宣稱矛盾。改 d=3.34 後 3d ＝ **10.02 ≥ 10**，截斷不觸發，兩份文件一致（見 v1.2.2 §4.1a）。短線（值域 ±2.5、3d ＝ 3）與波段（值域 ±5、3d ＝ 6）原本即不觸發，維持不變。

### 族 D 融資（S1 §A2.1 修訂版，有序）

> **`S(−r; 0, 5%)` 的 d=5% 不得三期間共用（2026-09-08 R5 修訂）**：`r` ＝融資餘額**期間**變化率，視窗 5／10／20 日（見上表「融資、借券視窗」欄），是 **n 日累積量**，三期間分布寬度不同。依 §B2.0「d 的校準政策」寫成 `d(n) ＝ 待 P2 校準`，三期間起點值暫皆為 **5%**（`calibrated=false`，僅供管線跑得起來；「目前相同」不等於「已判定相同」），P2 依 `d = p85 ÷ 3` 分期間重定並受 >15% 截斷閘門管轄。**序 1／序 2 的門檻 `|r| < 0.5%`／`r ≥ 0.5%` 屬情境門檻，依 §4.1a 一律用未截斷原值判定、不隨 d 改變。**
> **本族的 `N` 是恆等映射，不得再套 `N(v; 0, 100)`**（2026-09-08 更正：原寫「整段算完再過一次 `N(v;0,100)`」得 [13.53, 48.19]／[50.91, 68.23]，**與 §B2.0 政策第 1 點對照表「`S`＋`clip_3d` → 恆等映射」及第 7 點「不得重複套用」直接衝突，且與 S1 §A2.1 給出兩組不同分數**）。理由：序 3／4 的分數就是 `S(−r; 0, 5%)` 的直接輸出或其仿射變換，**原生值域即 [7.30, 92.70]**。序 4 的窄區間 [51.06, 71.35] 是**刻意的半幅壓縮**，**不是另一個原生值域**——拉回滿幅會把 `r=−0.5%` 的微幅資減映成 7.30（極空），規則用意全失。序 1／2 的離散 50 在任一讀法下都映到 50。

| 序 | 條件 | 分數 |
|---|---|---|
| 1 | **`\|r\| < 0.5%`（近零，最先判）** | 50 |
| 2 | `r ≥ 0.5%` 且期間報酬 > 0 | 50 |
| 3 | `r ≥ 0.5%` 且期間報酬 ≤ 0 | `S(−r; 0, 5%)` → 7.30–47.88 |
| 4 | `r ≤ −0.5%` | `50 + 0.5 × [S(−r; 0, 5%) − 50]` → 51.06–71.35 |

`r` ＝融資餘額期間變化率。**此族為候選假說**：融資減少不必然偏多，回測須分「價漲去槓桿」與「價跌去槓桿」兩組分別報告，未通過則權重歸零改純顯示。無信用交易資格 → 族缺。 **（2026-09-08 交叉檢查更正：序3 上界 47.88、序4 下界 51.06，原寫 50 為算術誤植）**

### 補充欄位（不進核心分）

**集保千張大戶持股比週變化**：P0-A §「待驗證 5」已驗證 SponsorYear 可取，首筆日期 2026-08-07 為**週五**（符合集保每週最後營業日慣例）。依 v1.2.2 §8，**只對候選名單與被查詢個股計算，不進共同核心分數**，作為個股頁的補充欄位。

**來源**：`TaiwanStockInstitutionalInvestorsBuySell`、`TaiwanStockMarginPurchaseShortSale`、`TaiwanDailyShortSaleBalances`、`TaiwanStockHoldingSharesPer`（補充）。`available_at` 一律 T 日晚間落地 → **T+1 開盤前可用**。

---

## B2.6 上爻｜外部支持

| 族 | 權重 | 子指標 | 轉換 | c | d | 方向 |
|---|---:|---|---|---|---|---|
| A 市場 | .50 | 所屬市場**該期間**的大盤方向分數（B1.7） | 直接沿用（該分數由已正規化的六爻加權而得，原生即 [7.30, 92.70]，**不再套 `N`**） | — | — | + |
| B 產業 | .50 | B1 產業期間相對大盤報酬（中位數） | S | 0 | 見下表 | + |
| | | B2 產業內站上 MA20 家數比 | `L(30%, 50%, 70%)` | — | — | + |

| 期間 | 產業視窗 / d |
|---|---|
| 短線 | 5 日 / 3pp |
| 波段 | 10 日 / 4pp |
| 中期 | 20 日 / 5pp |

族 B 內部：B1 .6、B2 .4。

**依賴順序（實作必須遵守）**：**大盤六爻必須先算完**，個股上爻才能算。批次順序：全市場價量 → 大盤六爻（加權、櫃買）→ 產業聚合 → 個股六爻。

**產業分類**：`TaiwanStockInfo.industry_category`（交易所產業別，互斥可加總）。產業鏈分類為多對多、會重疊，**不用於上爻**。

---

## B2.7 個股三期間方向權重（沿 v1.2.2 §5.2，未變）

| 面向 | 短線 | 波段 | 中期 |
|---|---:|---:|---:|
| 初 營運 | 5% | 10% | 20% |
| 二 趨勢 | 25% | 25% | 25% |
| 三 動能 | 20% | 20% | 15% |
| 四 量價 | 20% | 15% | 10% |
| 五 籌碼 | 20% | 20% | 15% |
| 上 外部 | 10% | 10% | 15% |

---

## B2.8 參數字典的機器可讀格式

P1 交付時每個子指標一列，欄位如下（本表即其人類可讀版）：

```
market, line, family, family_weight, horizon, indicator_id, window, formula,
unit, transform, c, d, clip_policy, native_range, direction, scored,
missing_rule, denominator_rule, source_dataset, available_at_rule,
calibrated, version
```

> **`market` 欄為 R5g 新增**：B1 §B1 明令「兩市場的參數必須從各自的參數表讀取、不得共用同一份設定物件」，§16.5 的「截斷比例」與「可達邊界」也都要求逐市場，但原字典**在格式上表達不了這個維度**。欄位全集與必要維度以 `P1-B5-dimensions.md` §B5.1／§B5.2 為準；**兩市場實算相同時仍須各存一列，不得省略**（§B5.0 規則 4）。

`native_range` ＝該轉換的原生值域，由框架據以套用一次 `N`（見 §B2.0「轉換值域統一」第 7 點）；`[7.30, 92.70]` 者為恆等映射。

`calibrated=false` 表示 c／d 為未校準的候選值——**目前全部子指標皆為 false**。P2 以實際分布校準後才可改 true，且校準過程須在**訓練段**完成（R5e 更正：原寫「驗證段」，與 §B2.0 政策第 3／4 點「母體定死為訓練段、校準只用訓練段資料」直接衝突——照原文實作就是拿驗證段調參，正是政策第 4 點列為「會使 §13 回測結論失效」的行為）、不得使用保留段。
