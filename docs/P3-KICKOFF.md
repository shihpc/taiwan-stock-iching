# P3 開工前置：前置狀態、完成定義、裁定紀錄

> **狀態：§5 八題已於 2026-09-16 由使用者裁定「全照建議」（`docs/P2-KICKOFF.md` 裁定 #48），本檔即開工文件。** 依 CANON 第 3 條，本檔在動任何一行
> P3 程式前寫成。體例沿 `docs/P2-KICKOFF.md`；盤點由 fresh-context 子代理實查（引用皆附 `檔案:行號`），主對話抽驗。

## 0. P3 的規格定義（v1.2.2 §14，逐字）

`spec/stock-iching-plan-v1.2.2.md:632`：
「| P3 驗證 | 三期間回測、成本敏感度、事件探索、前向紀錄 | 需核准：付費 AI（事件抽取） | §16.4–16.5；由使用者裁定上線範圍 |」

`docs/P2-KICKOFF.md:56`：「三期間回測、成本敏感度、事件探索 → **P3**」。

## 1. 前置狀態（P2 收尾與 P3 硬前置）

| # | 項目 | 現況 | 依據 |
|---|---|---|---|
| 1 | **預先登錄書凍結** | **未凍結**；凍結 commit 與 `model_version` 皆 TBD；順序已裁定「c／d 校準（只用訓練段）→ 生成卦別排序表 → 一次凍結」；全部 c／d／權重／門檻 `calibrated=false` | `docs/pre-registration.md:7-9`「填完並凍結後才可跑 P3」、`:16-19`、`:317`、`:323` |
| 2 | 歷史回補＋全量重播 | 已落地（Hetzner `scores.db` 1,618 日、9,070,896 列、2.6 GB；`fm-20260911-01`） | 裁定 #38（`docs/P2-KICKOFF.md:104`）、`docs/pre-registration.md:20` |
| 3 | 決定性重播（完成定義 #9） | 已於 Hetzner 全量實跑完成 | `docs/P2-REPLAY-PLAN.md:3` |
| 4 | 兩層 parity（D-3） | 第一輪已結（原料包逐位相同；分數差異根因＝除權息晚 7 日進檔，已重算覆蓋 09-01～09-15）；第二輪 09-25 後 | `docs/P2-DAILY-PLAN.md` §7.6.3 |
| 5 | 公告收集器（完成定義 #6／#7） | 2026-09-16 上線首班成功；#6 從第二個核對日起算、#7 需 3 個月（最快 2026-12） | `docs/P2-COLLECTOR.md` §5.3 |
| 6 | 完成定義 #5（連續 10 交易日班次留痕） | 未成立（收集器留痕 2 檔；每日班原料包 09-14 起算，第 10 日＝09-25） | `docs/P2-DAILY-PLAN.md:352` |
| 7 | **PIT 池** | 參考路徑（`scan_features.py`／`replay_io.ReplaySource.pool`）用**最新快照池套全部歷史**，非 PIT；只有流動性排名池是 PIT。兩份名單（可計分標的 vs 廣度母體）與轉板（11 檔同代號跨市場）尚未實作 | `docs/P2-DAILY-PLAN.md:517-519`、裁定 #6（`docs/P2-KICKOFF.md:72`）、`docs/pre-registration.md:46` |
| 8 | 回測統計層 | 本 repo **零實作**（無 IC／Newey-West／block bootstrap／分組報酬／成本模型）；裁定 #2 只借 `taiwan-backtest` 的 `block_boot_ci`＋`nw_se`（約 25 行），該 repo 目前不在 session 內 | 裁定 #2（`docs/P2-KICKOFF.md:68`）、`ls scripts/ src/iching/` |
| 9 | 回測資料出口 | 回測要用的 1,618 日分數只在 Hetzner `scores.db`（不進 git）；雲端 session 無 SSH。現有出口只有每日班種子（`export_seed.py`／`export_bundles.py`） | `spec/P1-B3-replay.md:64-67`、`CLAUDE.md` 已知坑 2 |
| 10 | 事件層歷史 | `data/events/` 只有 09-15 一日；回測期（2021-01～2026-08）**無事件版本鏈**，shadow IC 增量無歷史樣本可算，只能前向追蹤；付費 AI 抽取授權未確認 | `docs/P0A-report.md:231`、v1.2.2 `:616`、`:632` |
| 11 | 重現性環境 | Python ≥3.12（`sum()` Neumaier 影響 `scan.py:482` 邊界判定）只記在 `docs/P2-DAILY-PLAN.md:509-517`，未進規格／登錄書 | 同左 |

**結論**：P3 第一項工作不是回測，是**#1 校準與凍結**；而校準前要先解決 **#7 PIT 池**（否則訓練段分數含入池／存活者 look-ahead，
校準出來的 c／d 本身就被污染，凍結後保留段一經動用即消耗——`v1.2.2:729`）。#8／#9 是工程前置，可與 #7 並行。

## 2. 本 session 的執行限制

- 無 Hetzner SSH、無 `FINMIND_TOKEN`（`CLAUDE.md` 已知坑 2）；Hetzner 只在「資料只在那裡」或「長跑」時用，一句話貼（判準 §6）。
  校準與回測**都要讀 `scores.db`**，屬「資料只在 Hetzner」→ 兩條路：(a) 腳本在 Hetzner 跑、結果 commit 到分支；(b) 先做 #9 出口把
  訓練段所需欄位匯成 git 可容納的產物（估算見 §5 Q3）。
- `taiwan-backtest` 需 `add_repo` 才讀得到 `block_boot_ci`／`nw_se`。
- 付費 AI（事件抽取）未核准前一律不呼叫。

## 3. 完成定義（逐條可勾）

| # | 條件 | 怎麼驗 |
|---|---|---|
| 1 | 參考路徑池改 PIT（兩份名單＋轉板規則），`scores.db` 以 PIT 池重播、`data_version` 不變、`model_version` 因池語意變更而換版 | 合成世界：入池日 E 之前該檔不得出現在任何 T<E 的列；轉板檔在 T 日只出現在所屬市場；D-3 parity 對 09-14 後仍逐位相同 |
| 2 | 訓練段（2021-01-01～2023-06-30，603 日）c／d 校準完成，`calibrated=true` 的參數集＋校準報告（截斷比例 ≤15%、`d=p85÷3` 重定紀錄） | `spec/P1-B2-params.md` §B2.8 流程；§16.5 `:718` 截斷比例守門 |
| 3 | 卦別分組排序表由訓練段生成 | `docs/pre-registration.md` §1.6 |
| 4 | 預先登錄書一次凍結（含 #2 的 `model_version`、#3 排序表），凍結 commit 早於任何驗證段以後的結果 commit | `docs/pre-registration.md:17`、v1.2.2 `:728` |
| 5 | 統計層落地：IC（日 Spearman）、Newey-West（lag h／2h）、circular block bootstrap（block=max(21,3h)、1,000 次）、區塊數量近似值 <8 → 「證據不足」 | 對合成資料的已知答案；`block_boot_ci`／`nw_se` 借用處註明來源 commit |
| 6 | 成本層：個股（手續費 0.1425%×2、稅 0.3%、滑價 0.2% 基準＋{0.1,0.2,0.3}%、借券費 2%＋{1,2,4}%）、大盤 TX（§1.2.4 含轉倉） | 逐筆成本可追溯；敏感度表三欄 |
| 7 | 驗證段結果（三期間 IC、成本敏感度、穩健三段同號）＋§16.5 十六列守門全部零違規（purge／embargo／暖機隔離／標籤不越界） | 守門腳本輸出附在報告；每列 PASS |
| 8 | 保留段**只跑一次**、報告揭露波段／中期「證據不足」（裁定 T3）、多重比較 K=216 只揭露不校正（T8） | 報告章節對應 `docs/pre-registration.md` §1.4／§1.5 |
| 9 | 前向紀錄：每日班分數自凍結日起累積為前向樣本，格式與回測評估樣本同鍵 | `data/scores/<T>.json` 可直接進統計層 |
| 10 | 事件探索：只列研究假說、前向追蹤；歷史 shadow IC 增量標「無歷史樣本」 | v1.2.2 `:616`、`:326` |

## 4. 不在 P3 範圍

- 網站四入口／入口站卡／Worker 整合（P4）、盤中候選池（P5）。
- 事件層納入正式計分（第一版僅 shadow）。
- 對長 h 降門檻或提前保留段起點（§13.3b ②③ 已被裁定 T3 否決，只剩方向①等資料累積）。
- 改既有 repo（維持 P2 §4 的唯一解除）。

## 5. 裁定紀錄（2026-09-16，全照建議）

| Q | 問題 | 裁定（＝建議） |
|---|---|---|
| Q1 | **P3 與裁定 #43 的先後**：#43 說「P3 kickoff 前先做收集器」，收集器已上線但 #6 要一週、#7 要 3 個月。P3 是否以「收集器上線＋首週核對出爐」為門檻，不等 #7？ | 是——#7 只影響事件探索那段，回測本體不依賴 |
| Q2 | **PIT 池先於校準**：接受「校準前必須先把參考路徑改 PIT 並重播 1,618 日（Hetzner 約 12.6 小時）」？ | 是；否則校準結果帶 look-ahead，凍結後不能重來 |
| Q3 | **回測資料出口**：(a) 統計腳本在 Hetzner 跑、結果 commit；(b) 匯出訓練／驗證段所需欄位（分數＋前向報酬）到 git（估：1,618 日 × ~1,900 檔 × 3 horizon 的分數與標籤，壓縮後量級數十 MB，需實測）；(c) Release 資產 | (a) 為主、每次一句話貼；(b) 只匯評估要用的最小集合供雲端複算 |
| Q4 | **成本參數口徑**：滑價 0.1%（v1.2.2 §13.4）vs 0.2%（登錄書 §1.2 基準，自陳無出處） | 沿登錄書 0.2% 為基準、敏感度含 0.1%，報告明列覆蓋 |
| Q5 | **除權息處理**：登錄書 §3 自陳「P1 規格完全未定義」；290/2,139 檔全期無配息紀錄未核 | 回測報酬用還原價（裁定 #10 自算係數）；先抽 30 檔核對 290 檔的合理性 |
| Q6 | **付費 AI 事件抽取**是否在 P3 開通 | 不開；事件探索第一版只用「符合條款」欄的規則映射（v1.2.2 `:313`） |
| Q7 | **Python 版本約束**寫進登錄書 | 是，凍結 commit 記 3.12 |
| Q8 | **VIX 缺六年**：回測期大盤五爻永遠是 (乙) coverage 0.70 | 接受並在報告揭露；不補資料 |

## 6. 開工前必做的一件事

把 `CLAUDE.md` 的「目前 P1 規格完成、P2 未開工」改成實況（P2 收尾中：#5／#6／#7 未成立、其餘成立），並把 `docs/P2-KICKOFF.md` §6
「§1.2.1 標為提案」那句標為已被裁定 #8／#9 推翻——否則下一個 session 讀到的是被推翻的狀態。

## 7. 施工序列（裁定後）

1. **Q2 PIT 池**：先寫驗收條件（兩份名單、轉板、合成世界驗法），改參考路徑（`scan_features.py`／`replay_io`）與每日班同一份池語意，
   Hetzner 重播 1,618 日（一句話貼），D-3 對帳仍逐位。
2. **Q3 出口**：匯訓練／驗證段最小集合（分數＋前向報酬）到分支，量級實測後定。
3. 訓練段 c／d 校準 → 卦別排序表 → 登錄書一次凍結（含 Q7 Python 3.12）。
4. 統計層（借 `taiwan-backtest` 兩支函式）→ 驗證段 → 保留段只跑一次 → 報告。

